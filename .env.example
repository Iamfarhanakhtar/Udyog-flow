# UdyogFlow Database & Environment Configuration
# Copy this file to .env for local development

# PostgreSQL Connection String
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/udyogflow?schema=public"

# API Mode: "live" (connects to database/APIs) or "mock" (offline fallback)
NEXT_PUBLIC_API_MODE="live"
